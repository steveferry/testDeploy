<%= link_to 'New post', new_post_path%>

<table>
	<tr>
		<th>Title</th>
	</tr>
	<% @posts.each do |post|%>
		<tr>
			<td><%= link_to post.title, post%></td>
		</tr>
	<% end %>
</table>

<%= will_paginate @posts%>
